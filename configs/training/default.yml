training:
  epochs: 50
  batch_size: 32
  learning_rate: 0.001
  optimizer: "Adam"
  weight_decay: 0.0001
  scheduler_patience: 5
  early_stop_patience: 10
  save_dir: "checkpoints/"
  log_interval: 10
  seed: 42